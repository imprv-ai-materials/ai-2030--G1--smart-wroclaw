# CURRENT — which version of analytics_agent the app runs.
# The loader imports `api.ai.analytics_agent.versions.<version>` and uses its AGENT
# class. Promote by changing `version`; no code change.
version: v1
# Optional per-deployment model override for the phrasing step; null → config default.
model: null
