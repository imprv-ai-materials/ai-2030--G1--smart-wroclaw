# CURRENT — which version of report_agent the app runs.
# The loader imports `api.ai.report_agent.versions.<version>` and uses its AGENT
# class. Promote by changing `version`; no code change.
version: v1
# report_agent v1 is deterministic (no model); this override is unused for now.
model: null
