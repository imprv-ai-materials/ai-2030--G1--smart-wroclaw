# CURRENT — which version of main_agent the app runs.
# The loader imports `api.ai.main_agent.versions.<version>` and uses its AGENT class.
# Promote by changing `version`; no code change.
version: v1
# main_agent composes sub-agents (no model of its own); this override is unused.
model: null
