# CURRENT — which version of assistant_agent the app runs.
# The loader imports `api.ai.assistant_agent.versions.<version>` and uses its
# AGENT class. Promote by changing `version` (e.g. v1 → v2); no code change.
version: v1
# Optional per-deployment model override; null → the version's default / config.
model: null
