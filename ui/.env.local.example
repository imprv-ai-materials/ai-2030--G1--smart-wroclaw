# Template — copy to `ui/.env.local` (gitignored). Points the browser client at
# the backend REST API. Falls back to this same default in lib/api-client.ts if
# unset, so it's optional — kept here for clarity / easy overriding.
NEXT_PUBLIC_API_URL=http://localhost:8101/api/v1
